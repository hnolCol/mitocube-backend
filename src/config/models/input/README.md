## Input Types

This collection of models is intended to ensure that the required information that the frotnend should provide is in
a standardized format. Hence the backend defines which parameter/information the frontend should ask increasing 
the flexibility. 

The default is optimized for usage for BlueprintJS.

